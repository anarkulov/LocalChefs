package org.localchefs.app.shared.data.mapper

import org.localchefs.app.shared.data.dto.ChefProfileDto
import org.localchefs.app.shared.domain.model.ChefProfile

fun ChefProfileDto.toDomain(): ChefProfile = ChefProfile(
    id = id,
    name = name,
    bio = bio,
    address = address,
    city = city,
    state = state,
    zipCode = zip_code,
    bgImage = bg_image,
    cuisines = cuisines,
    deliveryDays = delivery_days,
    deliveryFee = delivery_fee,
    deliveryRadiusMiles = delivery_radius_miles,
    facebook = facebook,
    freeDeliveryAbove = free_delivery_above,
    instagram = instagram,
    isActive = is_active,
    isAgreementAccepted = is_agreement_accepted,
    isDeliveryAvailable = is_delivery_available,
    isFeatured = is_featured,
    isVerified = is_verified,
    latitude = latitude,
    longitude = longitude,
    location = location,
    minOrderNoticeHours = min_order_notice_hours,
    minimumOrderAmount = minimum_order_amount,
    rating = rating,
    reviewCount = review_count,
    telegram = telegram,
    type = type,
    whatsapp = whatsapp,
    createdAt = created_at,
    updatedAt = updated_at
)

fun ChefProfile.toDto(): ChefProfileDto = ChefProfileDto(
    id = id,
    name = name,
    bio = bio,
    address = address,
    city = city,
    state = state,
    zip_code = zipCode,
    bg_image = bgImage,
    cuisines = cuisines,
    delivery_days = deliveryDays,
    delivery_fee = deliveryFee,
    delivery_radius_miles = deliveryRadiusMiles,
    facebook = facebook,
    free_delivery_above = freeDeliveryAbove,
    instagram = instagram,
    is_active = isActive,
    is_agreement_accepted = isAgreementAccepted,
    is_delivery_available = isDeliveryAvailable,
    is_featured = isFeatured,
    is_verified = isVerified,
    latitude = latitude,
    longitude = longitude,
    location = location,
    min_order_notice_hours = minOrderNoticeHours,
    minimum_order_amount = minimumOrderAmount,
    rating = rating,
    review_count = reviewCount,
    telegram = telegram,
    type = type,
    whatsapp = whatsapp,
    created_at = createdAt,
    updated_at = updatedAt
) 